#include "main.h"
/**
 * add - add block
 *
 * Description:function that adds two integers and returns the result
 *
 * Return: the sum of numbers
 */
int add(int x, int y)
{
	int res;
	res = (x + y);
	return (res);
}
